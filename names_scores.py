def partition(left):
    # TODO try with rand
    # pivot = randrange(len(list))
    pivot = left


def quick_sort(list):
    partition(0)


with open('data/names.txt') as f:
    names = f.read().replace("\"", "").split(",")
    print(names)
